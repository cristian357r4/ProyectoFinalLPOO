<%= form_with(model: comida, local: true, class: 'form-group', validate: true) do |form| %>
  <% if comida.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(comida.errors.count, "error") %> prohibited this comida from being saved:</h2>

      <ul>
        <% comida.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= form.label :descripcion, role: 'label' %>
    <%= form.text_field :descripcion, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit 'Registrar' , class: 'btn btn-success'%>
  </div>
<% end %>
